import React from "react";

function Register() {
  return (
    <div className="page-card" aria-labelledby="register-title">
      <h1 id="register-title">Register</h1>
      <p>Placeholder registration page — capture name/email in future iterations.</p>
    </div>
  );
}

export default Register;