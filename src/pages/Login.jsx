import React from "react";

function Login() {
  return (
    <div className="page-card" aria-labelledby="login-title">
      <h1 id="login-title">Login</h1>
      <p>Placeholder login form UI — no auth implemented as per instructions.</p>
    </div>
  );
}

export default Login;